# Leave empty for same-origin /api rewrite to Nest (recommended).
# NEXT_PUBLIC_API_URL=
# Server-side rewrite target (Next → Nest). Defaults to http://localhost:4000.
API_INTERNAL_URL=http://localhost:4000

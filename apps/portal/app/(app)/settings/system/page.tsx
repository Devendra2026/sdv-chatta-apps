import Placeholder from "@/components/placeholder-page"

export default function Page() {
  return (
    <Placeholder
      title="System Settings"
      description="ULB branding, payment gateway mode, and retention policies — configure via environment for now."
    />
  )
}
